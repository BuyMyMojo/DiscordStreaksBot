# DiscordStreaks
this is a so so bot made to track streaks on discord like a less smart snapchat

##missing file
make sure to create a file called _config.yml in /DiscordStreaks in this format:
```yml
prefix: +!
tokens:
    discord: [discord token from dev portal]
```
